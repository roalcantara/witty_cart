class WittyCart.Env
  constructor: ->
    @$env = '<%= WittyCart.env %>'

  production: () ->
    @$env == 'production'

$(document).on 'turbolinks:load', ->
  window.WittyCart.env = new WittyCart.Env()
